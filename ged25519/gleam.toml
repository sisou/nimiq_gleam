name = "ged25519"
version = "1.0.2"

description = "Ed25519 signature functions - Gleam bindings for the Elixir `ed25519` package."
licences = ["Apache-2.0"]
repository = { type = "github", user = "sisou", repo = "nimiq_gleam" }

[dependencies]
gleam_stdlib = ">= 0.44.0 and < 2.0.0"
ed25519 = ">= 1.4.3 and < 2.0.0"

[dev-dependencies]
gleeunit = ">= 1.0.0 and < 2.0.0"
